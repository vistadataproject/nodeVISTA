/*global define*/
define([
   'jquery',
   'underscore',
   'backbone'
], function ($, _, Backbone) {
   'use strict';

   var MVDMEventModel = Backbone.Model.extend({

   });

   return MVDMEventModel;
});