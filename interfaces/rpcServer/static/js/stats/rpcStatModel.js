/*global define*/
define([
   'jquery',
   'underscore',
   'backbone'
], function ($, _, Backbone) {
   'use strict';

   //attributes: name, count, runner

   var RPCStatModel = Backbone.Model.extend();

   return RPCStatModel;
});